# Pinned production requirements (P1 audit-final, 2026-06).
# Сгенерированы из текущего рабочего venv (pip freeze + ручная фильтрация dev-only).
# Источник правды для верхних границ — pyproject.toml [project].dependencies +
# [project.optional-dependencies].{browser,web}. Dev-зависимости (pytest, ruff, httpx2)
# не включены — для них pip install -e ".[dev]".
#
# Регенерация после изменения pyproject:
#   python -m venv .venv-fresh && . .venv-fresh/bin/activate
#   pip install -e ".[browser,web]"
#   pip freeze | grep -vE '^(toursearch|pip-audit|cyclonedx|tomli-w)' > requirements.txt

# Core
pydantic==2.13.4
pydantic_core==2.46.4
annotated-types==0.7.0
typer==0.20.0
click==8.4.1
colorama==0.4.6

# Web (FastAPI + uvicorn + Jinja)
fastapi==0.136.3
uvicorn==0.41.1
starlette==0.50.0
jinja2==3.1.6
MarkupSafe==3.0.3
python-multipart==0.0.20
anyio==4.13.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.18
sniffio==1.3.1
certifi==2026.5.20
charset-normalizer==3.4.7

# Browser (Playwright)
playwright==1.60.0
pyee==13.0.1
greenlet==3.5.1

# Misc transit
shellingham==1.5.4
rich==14.3.0
markdown-it-py==4.2.0
mdurl==0.1.2
Pygments==2.20.0
typing_extensions==4.16.1
